application.title=@PROJECT_NAME@

src.dir=src
build.dir=build
tools.dir=tools

theme.name=@THEME@
theme.package=@THEME_PACKAGE@

# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=@PROJECT_NAME@.jar
 