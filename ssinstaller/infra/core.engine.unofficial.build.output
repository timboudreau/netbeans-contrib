
-pre-init:

-init-private:

-init-user:

-init-project:

-init-macrodef-property:

-do-init:

-post-init:

-init-check:

-init-macrodef-javac:

-init-macrodef-junit:

-init-macrodef-nbjpda:

-init-debug-args:

-init-macrodef-debug:

-init-macrodef-java:

-init-presetdef-jar:

init:

deps-jar:

-pre-pre-compile:

-pre-compile:

-do-compile:

define-platform:

init-windows:

init-linux:

init-solaris-x86:

init-solaris-sparc:

init-macosx:

init-platform:

jnilib-init:

jnilib-clean:

jnilib-compile:

jnilib-build:

launcher-init:

launcher-clean:

launcher-compile:

launcher-build:

cleaner-init:

cleaner-clean:

cleaner-compile:

cleaner-build:

native-build:

copy-native:

copy-jnilibs:

copy-launchers:

copy-windows-launcher:

copy-unix-launcher:

copy-cleaners:

build-custom-tasks:
   [delete] Deleting directory /local/ws/installer/main/nbi/infra/build/.ant-lib/build/classes
    [mkdir] Created dir: /local/ws/installer/main/nbi/infra/build/.ant-lib/build/classes
    [javac] Compiling 19 source files to /local/ws/installer/main/nbi/infra/build/.ant-lib/build/classes

init-custom-tasks:

create-jar-contents-list:
[write-jar-content-list] Root directory : /local/ws/installer/main/nbi/engine/build/classes
[write-jar-content-list] Output file : /local/ws/installer/main/nbi/engine/build/classes/data/engine.list
[write-jar-content-list] Mask  : (^native/.*|^org/.*|^com/apple/.*|^data/|^data/engine(_[a-zA-Z]+)*\.properties)

-post-compile:

compile:
