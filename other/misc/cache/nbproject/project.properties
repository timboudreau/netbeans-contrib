application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/cache.jar
dist.javadoc.dir=${dist.dir}/javadoc
jar.compress=false
javac.classpath=\
    ${reference.storage.jar}:\
    ${reference.numeric_collections.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=${default.javac.source}
javac.target=${default.javac.target}
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}

javadoc.author=true
javadoc.encoding=UTF-8
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=Cache API
javadoc.docfiles=${basedir}/doc/api
javadoc.overview=${basedir}/doc/api/overview.html
javadoc.main.page=${basedir}/doc/api/overview.html

platform.active=default_platform
project.numeric_collections=../numeric_collections
project.storage=../storage
reference.numeric_collections.jar=${project.numeric_collections}/dist/numeric_collections.jar
reference.storage.jar=${project.storage}/dist/storage.jar
run.classpath=\
    ${javac.classpath}:\
    ${reference.numeric_collections.jar}:\
    ${reference.storage.jar}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
